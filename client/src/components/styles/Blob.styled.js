import styled from "styled-components";

const StyledBlob = styled.div`
  width: fit-content;
  position: fixed;
  z-index: -1;
  left: 0;
  top: 0;
  svg {
    width: 700px;
  }
`;

export default StyledBlob;
