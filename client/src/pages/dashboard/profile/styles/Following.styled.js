import styled from "styled-components";

const StyledFollowing = styled.div`
  .following-parent {
    display: flex;
    flex-wrap: wrap;
    .user {
      margin-right: 10px;
      margin-bottom: 10px;
    }
  }
`;

export default StyledFollowing;
